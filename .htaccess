<IfModule mod_rewrite.c>
    RewriteEngine On

    # If document root is the project folder (not /public), route everything into public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
